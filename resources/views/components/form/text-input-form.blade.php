<div class="form-group">
  <label for="{{ $name }}" class="text-label_light">{{ $label }}</label>
  <input type="text" name="{{ $name }}" id="{{ $name }}" class="form-control-input" placeholder="{{ $label }}">
</div>
